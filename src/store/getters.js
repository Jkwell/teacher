export const isLogin = state => state.loginStatus.isLogin

export const userId = state => state.loginStatus.userId

export const userInfo = state => state.userInfo

export const phone = state => state.phone

export const emali = state => state.emali

export const pwd = state => state.pwd

export const avatar = state => state.avatar
